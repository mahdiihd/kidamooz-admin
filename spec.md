# مشخصات پروژه پنل مدیریت

این سند، وضعیت مشاهده‌شده در کد را توضیح می‌دهد و سند نیازمندی‌های آینده نیست. نام فایل مطابق درخواست `spec.md` است.

## نقش و فناوری

پنل مدیریت محتوای کیدآموز با Angular 21.2، TypeScript 5.9، RxJS و SCSS ساخته شده است. مسیرها مؤلفه‌ها را به‌صورت تنبل بارگذاری می‌کنند. آزمون‌ها از سازوکار Angular و Vitest استفاده می‌کنند.

## قابلیت‌های موجود در مسیرها

ورود، داشبورد، دسته‌بندی‌ها، قصه‌ها، بررسی قصه‌های ارسالی، اعضا، چالش‌ها، کاربران مدیریتی، ارسال اعلان و گزارش فعالیت‌ها در `src/app/app.routes.ts` تعریف شده‌اند. ورود با محافظ‌های مهمان و کاربر کنترل می‌شود.

## نقشه کد

- `src/app/features`: صفحات هر قابلیت.
- `src/app/core/services`: احراز هویت، محتوا، اعضا، رسانه، اعلان و داده‌های mock.
- `src/app/core/models`: مدل‌های سمت کاربر.
- `src/app/core/guards` و `src/app/core/interceptors`: حفاظت مسیر و پردازش درخواست‌ها.
- `src/app/layout` و `src/app/shared`: پوسته و اجزای مشترک.
- `src/app/app.config.ts`: پیکربندی برنامه.
- `angular.json`: پیکربندی ساخت و آزمون و محدودیت حجم خروجی.

## اجرا و ارتباط با سرور

دستورات از پوشه `Admin` اجرا می‌شوند:

```powershell
npm ci
npm start
npm run build
npm test -- --watch=false
```

اسکریپت شروع روی پورت ۴۲۰۰ و تمام رابط‌های شبکه گوش می‌دهد. مقدار API و حالت mock را از تنظیم محیط بخوانید. فایل محیط توسعه `src/environments/environment.ts` و فایل جایگزین production، `src/environments/environment.production.ts` است.

قرارداد واقعی را در پروژه بک‌اند بررسی کنید؛ [راهنمای اتصال](../Back/docs/ADMIN_INTEGRATION.md) نقطه شروع است و در صورت اختلاف، کد جاری ملاک قرار می‌گیرد. تغییرات مشترک API می‌توانند بر اپ موبایل نیز اثر بگذارند.

## اعتبارسنجی

برای تغییرات اجرایی، ساخت production و آزمون متناسب با رفتار تغییرکرده را اجرا کنید. در رابط، ورود، حالت بارگذاری، خطا، فهرست خالی و چیدمان فارسی را بررسی کنید. این سند ادعای موفقیت ساخت یا سلامت کامل پروژه ندارد.

### جلد رایگان هنگام تأیید قصه

در صف بررسی، درخواست کاور رایگان مشخص است. مدیر فایل جلد را در بخش «جلد هنگام انتشار» انتخاب می‌کند؛ هنگام تأیید ابتدا فایل از مسیر رسانه موجود بارگذاری می‌شود، سپس URL همراه درخواست تأیید ارسال می‌شود. خطای بارگذاری مانع تأیید است. برای درخواست رایگان، جلد جایگزین الزامی است.
